version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mysql-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: portal_gaz_db

    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    networks:
      - portal-network

  portalrh-app:
    image: portalrh-app-image
    container_name: portalrh-app
    restart: on-failure
    ports:
      - "8090:8090"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/portal_gaz_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root
      RECOMMENDATION_API_URL: http://recommendation-api:8000/recommend
    depends_on:
      - mysql
      - recommendation-api
    networks:
      - portal-network

  recommendation-api:
    build:
      context: ./recommendation-api
    container_name: recommendation-api
    restart: on-failure
    ports:
      - "8000:8000"
    networks:
      - portal-network

volumes:
  mysql-data:

networks:
  portal-network:
    driver: bridge
